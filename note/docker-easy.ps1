docker stop mysql-demo; docker rm mysql-demo; docker run -d --name mysql-demo --network demo-net -e MYSQL_ROOT_PASSWORD=123456 -e MYSQL_DATABASE=test_db -p 3306:3306 -v mysql-data:/var/lib/mysql mysql:8.0; Write-Host "等待 MySQL 启动..." -ForegroundColor Yellow; Start-Sleep -Seconds 15; docker exec mysql-demo mysql -u root -p123456 test_db -e "CREATE TABLE IF NOT EXISTS user (id BIGINT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(50) NOT NULL, age INT NOT NULL); INSERT IGNORE INTO user (name, age) VALUES ('Zhang San', 25), ('Li Si', 30), ('Wang Wu', 28); SELECT * FROM user;"
docker stop redis-demo; docker rm redis-demo; docker run -d --name redis-demo --network demo-net -p 6379:6379 redis:alpine; docker stop demo-app; docker rm demo-app; docker run -d --name demo-app --network demo-net -e "SPRING_DATASOURCE_URL=jdbc:mysql://mysql-demo:3306/test_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai" -e SPRING_DATASOURCE_USERNAME=root -e SPRING_DATASOURCE_PASSWORD=123456 -e SPRING_DATA_REDIS_HOST=redis-demo -e SPRING_DATA_REDIS_PORT=6379 -p 8080:8080 demo:1.0; docker logs -f demo-app
